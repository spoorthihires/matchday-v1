import './employerBase.js';

// Placeholder for Task 4 (routing scaffold). Task 8 replaces this with the ported
// dashboard fed by useEmployerPortal() (Matchday_Employer.html lines ~2705-2790),
// rendered inside the Task 7 EmployerShell.
export function EmployerDashboard() {
  return (
    <div className="employer-app">
      <div className="wrap" style={{ padding: '48px 0' }}>
        <h1>Employer Dashboard</h1>
      </div>
    </div>
  );
}
